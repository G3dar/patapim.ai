---
export const prerender = false;

import BaseLayout from '../../layouts/BaseLayout.astro';
import Navbar from '../../components/Navbar.astro';
import Footer from '../../components/Footer.astro';
import { getLocaleFromUrl, t, getLocalePath } from '../../i18n';
import en from '../../i18n/translations/en';
import ja from '../../i18n/translations/ja';

const locale = getLocaleFromUrl(Astro.url);
const lp = (path: string) => getLocalePath(locale, path);
const translations = locale === 'ja' ? ja : en;

const { code } = Astro.params;
const env = Astro.locals.runtime.env;

let status: 'valid' | 'invalid' | 'claimed' | 'expired' = 'invalid';
let invite: any = null;

if (code) {
  try {
    const raw = await env.LICENSES.get(`beta-invite:${code}`);
    if (raw) {
      invite = JSON.parse(raw);
      if (invite.claimedBy) {
        status = 'claimed';
      } else if (new Date(invite.expiresAt) < new Date()) {
        status = 'expired';
      } else {
        status = 'valid';
      }
    }
  } catch {
    // KV lookup failed — treat as invalid
  }
}

// Set beta cookie if valid (same pattern as __patapim_ref in referral system)
if (status === 'valid' && code) {
  Astro.cookies.set('__patapim_beta', code, {
    path: '/',
    secure: true,
    sameSite: 'lax',
    maxAge: 60 * 60 * 24 * 7, // 7 days
  });
}
---

<BaseLayout title={t(locale, 'meta.betaTitle')} description={t(locale, 'meta.betaDescription')}>
  <Navbar />

  <main class="beta-page">
    <div class="container">
      {status === 'invalid' && (
        <div class="beta-card">
          <div class="beta-icon error-icon">
            <svg width="56" height="56" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/>
              <line x1="15" y1="9" x2="9" y2="15"/>
              <line x1="9" y1="9" x2="15" y2="15"/>
            </svg>
          </div>
          <h1 class="beta-title">{t(locale, 'beta.invalidTitle')}</h1>
          <p class="beta-desc">{t(locale, 'beta.invalidDesc')}</p>
          <div class="beta-cta">
            <a href={lp('/')} class="tv-pbtn beta-btn">{t(locale, 'common.goToHomepage')}</a>
          </div>
        </div>
      )}

      {status === 'claimed' && (
        <div class="beta-card">
          <div class="beta-icon warn-icon">
            <svg width="56" height="56" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/>
              <line x1="12" y1="8" x2="12" y2="12"/>
              <line x1="12" y1="16" x2="12.01" y2="16"/>
            </svg>
          </div>
          <h1 class="beta-title">{t(locale, 'beta.claimedTitle')}</h1>
          <p class="beta-desc">{t(locale, 'beta.claimedDesc')}</p>
          <div class="beta-cta">
            <a href={lp('/')} class="tv-pbtn beta-btn">{t(locale, 'common.goToHomepage')}</a>
          </div>
        </div>
      )}

      {status === 'expired' && (
        <div class="beta-card">
          <div class="beta-icon warn-icon">
            <svg width="56" height="56" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <circle cx="12" cy="12" r="10"/>
              <polyline points="12 6 12 12 16 14"/>
            </svg>
          </div>
          <h1 class="beta-title">{t(locale, 'beta.expiredTitle')}</h1>
          <p class="beta-desc">{t(locale, 'beta.expiredDesc')}</p>
          <div class="beta-cta">
            <a href={lp('/')} class="tv-pbtn beta-btn">{t(locale, 'common.goToHomepage')}</a>
          </div>
        </div>
      )}

      {status === 'valid' && (
        <div class="beta-card beta-card-valid">
          <div class="beta-badge">{t(locale, 'beta.badge')}</div>
          <h1 class="beta-title">{t(locale, 'beta.title')}</h1>
          <p class="beta-value">{t(locale, 'beta.freeProLifetime')} <span class="beta-value-note">{t(locale, 'beta.value')}</span></p>

          <ul class="beta-features">
            {translations.beta.features.map((feature) => (
              <li>
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>
                {feature}
              </li>
            ))}
          </ul>

          <a href={`/api/auth/google?beta=${code}`} class="tv-pbtn tv-pbtn-accent beta-cta-btn">
            {t(locale, 'beta.claimButton')}
          </a>

          <p class="beta-note">{t(locale, 'beta.claimHint')}</p>
        </div>
      )}
    </div>
  </main>

  <Footer />
</BaseLayout>

<style>
  .beta-page {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 120px 0 80px;
  }

  .beta-card {
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    max-width: 520px;
    width: 90%;
    margin: 0 auto;
    padding: var(--space-4xl) var(--space-3xl);
    background: linear-gradient(180deg, #0b0b0e 0%, #070708 100%);
    border: 1px solid rgba(255, 255, 255, 0.09);
    box-shadow: 0 18px 48px rgba(0, 0, 0, 0.55), inset 0 1px 0 rgba(255, 255, 255, 0.04);
  }

  .beta-card-valid {
    border-color: rgba(139, 124, 248, 0.45);
    box-shadow:
      0 0 44px rgba(139, 124, 248, 0.13),
      0 0 90px rgba(34, 211, 238, 0.07),
      inset 0 1px 0 rgba(255, 255, 255, 0.04);
  }

  .beta-card-valid::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 2px;
    background: linear-gradient(90deg, #c26bf0, #8b7cf8, #22d3ee);
    opacity: 0.9;
  }

  .beta-icon {
    margin-bottom: var(--space-xl);
  }

  .error-icon {
    color: var(--error);
  }

  .warn-icon {
    color: var(--warning);
  }

  .beta-badge {
    display: inline-block;
    font-family: var(--font-mono);
    font-size: 10px;
    font-weight: 700;
    color: #000;
    background: linear-gradient(90deg, #c26bf0, #8b7cf8, #22d3ee);
    padding: 4px 16px;
    border-radius: 0;
    margin-bottom: var(--space-lg);
    letter-spacing: 0.2em;
    text-transform: uppercase;
  }

  .beta-title {
    font-size: 24px;
    font-weight: 700;
    color: var(--text-primary);
    margin-bottom: var(--space-lg);
    line-height: 1.3;
  }

  .beta-value {
    font-size: 15px;
    font-weight: 700;
    background: linear-gradient(120deg, #c26bf0, #8b7cf8, #22d3ee);
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    margin-bottom: var(--space-2xl);
  }

  .beta-value-note {
    font-size: 12.5px;
    font-weight: 400;
    background: none;
    -webkit-background-clip: initial;
    background-clip: initial;
    -webkit-text-fill-color: var(--text-tertiary);
    color: var(--text-tertiary);
  }

  .beta-desc {
    font-size: 14px;
    color: var(--text-secondary);
    line-height: 1.7;
    margin-bottom: var(--space-2xl);
  }

  .beta-features {
    list-style: none;
    text-align: left;
    width: 100%;
    margin: 0 0 var(--space-2xl);
    padding: 0;
  }

  .beta-features li {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 10px 0;
    font-size: 13.5px;
    color: var(--text-secondary);
    border-bottom: 1px solid var(--border-subtle);
  }

  .beta-features li:last-child {
    border-bottom: none;
  }

  .beta-features svg {
    color: var(--cy);
    flex-shrink: 0;
  }

  .beta-cta-btn {
    width: 100%;
    font-size: 14px;
    padding: 14px 32px;
  }

  .beta-btn {
    min-width: 220px;
  }

  .beta-note {
    font-size: 12px;
    color: var(--text-muted);
    margin-top: var(--space-lg);
  }

  .beta-cta {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--space-lg);
  }

  @media (max-width: 480px) {
    .beta-card {
      padding: var(--space-2xl) var(--space-xl);
    }
    .beta-title {
      font-size: 20px;
    }
  }
</style>
